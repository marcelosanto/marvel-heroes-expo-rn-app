import { StatusBar, StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    width: "100%",
    flexDirection: "row",
    backgroundColor: "red",
    marginLeft: 40,
  },
  textTitle: {
    fontSize: 30,
    fontWeight: "bold",
  },
});
